services:

  zookeeper:
    image: confluentinc/cp-zookeeper:7.6.1
    container_name: vanguard-zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "2181"]
      interval: 10s
      timeout: 5s
      retries: 5

  kafka:
    image: confluentinc/cp-kafka:7.6.1
    container_name: vanguard-kafka
    depends_on:
      zookeeper:
        condition: service_healthy
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
    healthcheck:
      test: ["CMD", "kafka-broker-api-versions", "--bootstrap-server", "localhost:9092"]
      interval: 15s
      timeout: 10s
      retries: 10

  postgres:
    image: postgres:16-alpine
    container_name: vanguard-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: vanguard
      POSTGRES_DB: vanguard
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: vanguard-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - redis_data:/data


  # vitals:
  #   build:
  #     context: .
  #     dockerfile: services/vitals/Dockerfile
  #   container_name: vanguard-vitals
  #   depends_on:
  #     kafka:
  #       condition: service_healthy
  #     postgres:
  #       condition: service_healthy
  #   ports:
  #     - "3003:3003"
  #   environment:
  #     VITALS_SERVICE_PORT: 3003
  #     VITALS_SERVICE_URL: 0.0.0.0
  #     KAFKA_HOST: kafka
  #     KAFKA_PORT: 29092
  #     KAFKA_CLIENT_ID: vitals-service
  #     KAFKA_GROUP_ID: vitals-service
  #     KAFKA_TOPIC: vitals
  #     DATABASE_HOST: postgres
  #     DATABASE_PORT: 5432
  #     DATABASE_USERNAME: postgres
  #     DATABASE_PASSWORD: postgres
  #     DATABASE_NAME: vitals

  # api-gateway:
  #   build:
  #     context: .
  #     dockerfile: apps/api-gateway/Dockerfile
  #   container_name: vanguard-api-gateway
  #   depends_on:
  #     - vitals
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     API_GATEWAY_PORT: 3000
  #     VITALS_SERVICE_URL: vitals:3003

volumes:
  postgres_data:  
  redis_data:
